local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "My Custom Hub | Kiew Testing",
   LoadingTitle = "กำลังโหลดระบบทดสอบ...",
   LoadingSubtitle = "by Kiew",
   ConfigurationSaving = {
      Enabled = false, -- ปิดไว้ก่อนจะได้ไม่เปลืองพื้นที่
      FolderName = nil,
      FileName = "TestingConfig"
   },
   Discord = {
      Enabled = false
   },
   KeySystem = false
})

-- สร้างแท็บเริ่มต้น 1 แท็บ
local HomeTab = Window:CreateTab("หน้าแรก", 4483362458)

-- สร้างหัวข้อเริ่มต้น
local Section = HomeTab:CreateSection("สถานะการทดสอบ")

-- สร้างปุ่มทดสอบ
HomeTab:CreateButton({
   Name = "ปุ่มทดสอบระบบ (Test Button)",
   Callback = function()
       Rayfield:Notify({
          Title = "การทดสอบสำเร็จ!",
          Content = "UI ใช้งานได้ปกติแล้วครับ Kiew!",
          Duration = 5,
       })
   end,
})

-- โหลดการตั้งค่า (ถ้ามี)
Rayfield:LoadConfiguration()
